# AI 应用聚合平台产品需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | AI 应用聚合平台 |
| 项目目录 | Aggregation_Platform |
| 技术现状 | React 18 + Vite 5 + TypeScript 5 + **Material UI 6**（主要组件库） + Ant Design 6（ConfigProvider 主题层） |
| 文档版本 | V1.2 |
| 编写日期 | 2026-06-12 |
| 适用对象 | 产品、研发、测试、运维、企业管理员、超级管理员 |

## 2. 产品背景

AI 应用聚合平台面向企业内部 AI 能力统一入口建设。平台已完成门户首页、应用中心、登录认证、权限管理、应用授权、组织管理和监控总览的完整前端实现，当前数据层使用前端 mock 与 `localStorage` 持久化，适合原型演示和离线部署验证。后续重点为接入后端 API、补齐 Token 配额管理和系统日志等功能。

## 3. 当前项目已实现功能

### 3.1 门户前台

#### 门户首页 `/`

由 `TopNav`、`HeroBanner`、`AppMatrix`、`ValueSection` 组成：

- **顶部导航**：Logo、首页、应用中心、平台优势导航；全局应用搜索（命中后高亮滚动定位）；已登录时展示用户头像下拉菜单，未登录时展示登录按钮。
- **Hero 区**：平台名称、定位文案、「浏览全部应用」和「了解平台价值」跳转按钮；深蓝渐变 + 浮动光斑背景。
- **应用矩阵**：卡片形式展示 AI 应用，支持分类筛选（全部 / 研发提效 / 硬件设计 / 办公协同），点击跳转到对应应用地址；登录后仅展示当前用户被授权的应用，超级管理员默认看到全部应用。
- **平台价值**：展示平台覆盖、安全合规、稳定保障、持续迭代等价值点。

#### 应用中心 `/apps`

- 展示当前用户有权访问的应用（super_admin 看全部）；未授权用户显示提示 Alert。
- 支持关键词搜索（名称、描述、分类）、分类筛选（ToggleButtonGroup）、排序（最近使用 / 推荐应用 / 热门应用）。
- 应用卡片展示图标、名称、分类 Chip、描述、「立即体验」按钮；未登录时点击跳转登录页。

#### 登录页 `/login`

- 左右分栏卡片布局（宽 980px）：左侧品牌面板（深蓝渐变，企业级 AI 能力介绍），右侧登录表单。
- **账号密码登录**：账号、密码字段，「记住我」Checkbox，错误次数 ≥2 次后显示图形验证码（固定值 A8K3 用于演示），连续失败 5 次提示账号锁定。
- **忘记密码弹窗**：4 步 Stepper（输入邮箱 → 验证码 → 设置新密码 → 完成），含验证码 60 秒倒计时重发、邮箱格式校验、密码强度校验（≥8 位含大写字母和数字）、两次密码一致校验。
- **找回账号弹窗**：输入企业邮箱，区分单账号、多账号、未找到三种反馈。
- 默认账号 admin（映射超级管理员 admin1），快捷密码 `admin`；重置后密码为 `Avic@12345678`。

### 3.2 管理后台（需登录，共用 AdminLayout）

AdminLayout：左侧常驻侧边栏（248px）+ 顶部面包屑工具栏（64px）+ 右侧内容区（padding 32px）。侧边栏展示按角色过滤的导航分组，顶部工具栏展示面包屑、当前用户姓名/所属企业和退出登录按钮。

#### 应用管理 `/admin/apps`（仅 super_admin 可用）

- 应用列表表格：名称、分类、描述、跳转网址、授权人数 Chip（未授权 / N 人 / 全员可见）、启停 Switch、操作列（编辑 / 授权 / 删除）。
- 搜索和筛选栏：关键词搜索、分类下拉、状态下拉。
- **新增/编辑对话框**：应用名称、分类、图标（含图标预览下拉）、启停 Switch、描述（多行）、跳转网址（含格式校验）。
- **授权用户对话框**：按企业 → 部门 → 用户三级树形 Checkbox 批量选择；支持企业全选、部门全选、单人勾选；全局「全部企业全选」和「全部清空」快捷操作；已选计数 Chip 实时反馈。
- 删除对话框：需二次确认，提示"已授权用户立即无法访问"。

#### 用户管理 `/admin/users`

- 数据可见范围受角色约束：super_admin 看全部企业，enterprise_admin 仅看本企业，department_admin 仅看本部门。
- 用户列表表格：头像+姓名、账号、邮箱、所属企业（Chip）、部门、角色（Chip）、密码列（仅 super_admin 可见，支持眼睛图标查看/隐藏、复制）、启停 Switch、操作列（编辑 / 重置密码 / 删除）。
- 筛选栏：关键词搜索（姓名/账号/邮箱）、企业下拉（仅 super_admin）、部门下拉、状态下拉；分页（5/10/20/50 条/页）。
- **角色层级约束**：操作者只能管理权限级别严格低于自身的用户；不能管理自己（除编辑自身信息外）；不能删除/禁用自己；enterprise_admin 不能管理运维人员；department_admin 只能管理普通用户。
- **新增/编辑对话框**：账号（新建可填、编辑只读）、显示名、邮箱、企业（enterprise_admin/dept_admin 锁定为本企业）、部门（dept_admin 锁定为本部门）、角色下拉（受层级约束，无权限的选项显示"无分配权限"并禁用）、启停 Switch；super_admin 还可直接编辑密码并重置为初始密码。
- **重置密码确认框**：展示将被重置的初始密码（`Avic@12345678`），要求管理员告知用户登录后及时修改。
- 删除确认对话框：需二次确认。

#### 角色管理 `/admin/roles`

- 角色列表（左 60%）+ 权限配置侧边区（右 40%）布局；选中某角色后右侧展示其权限树。
- **角色列表**：名称、编码、所属企业、绑定用户数、创建人、状态、更新时间、操作（查看/编辑/复制/停用/删除/查看权限审计）。
- 筛选：关键词搜索、状态下拉、企业下拉（仅 super_admin）。
- **权限树**（三级：模块 → 功能 → 操作）：可视化 Checkbox，支持父子级联勾选、全选/反选，搜索权限节点自动展开路径；冲突权限自动检测并阻止保存（展示冲突节点和原因）。
- **保存确认对话框**：展示变更摘要（新增 N 项、移除 N 项）和受影响用户数，管理员确认后保存并实时生效（写入 `aap.rolePermissions`）。
- **权限审计时间轴**：可弹出对话框查看历史变更记录（操作时间、操作人、变更内容）；支持按角色、操作人、时间范围筛选。

#### 组织管理 `/admin/organizations`

三个 Tab 页切换：

- **企业管理**：企业列表（名称、编码、描述、联系人、状态、创建时间），支持新增、编辑、停用、删除；super_admin 可跨企业操作，enterprise_admin 仅可查看本企业。
- **部门管理**：部门列表（名称、编码、所属企业、负责人、状态），支持新增、编辑、停用、删除；支持按企业筛选，enterprise_admin 只看到本企业部门。
- **权限下放**（Delegate）：由 super_admin 向其他角色（enterprise_admin / department_admin / ops）授予特定模块的额外权限；选择目标角色和可下放权限集合后确认保存，调用 `setRolePermissions` 实时生效。

#### 监控总览 `/ops/monitor`

- 4 张指标卡片：应用总数、注册用户数、总调用次数、活跃用户数（当前等待真实埋点数据接入，显示"—"）。
- 时间维度切换：今日 / 近 7 天 / 近 30 天（ToggleButtonGroup）。
- 应用使用次数表（7 列 Grid）：应用名、调用次数、独立用户数、最近使用时间；数据为空时展示空状态。
- 用户活跃度表（5 列 Grid）：用户名+所属企业、登录次数、应用使用次数、最近活跃时间；数据为空时展示空状态。

### 3.3 用户功能下拉菜单（UserMenu）

点击顶部导航右侧用户头像展开：

| 菜单区 | 内容 |
| --- | --- |
| 用户信息区 | 头像、显示名、企业 · 部门、角色 Chip（蓝色 outlined） |
| 管理功能（按角色过滤） | 权限管理（→ `/admin/roles`，enterprise_admin / super_admin 可见）；监控运维（→ `/ops/monitor`，ops / super_admin 可见） |
| 底部 | 退出登录（清除 session 并跳回门户首页） |

> 注：「个人中心」「我的 Token 用量」「配额中心」「操作日志」入口尚未实现，当前 UserMenu 仅包含上表中的管理功能和退出登录。

### 3.4 当前能力边界

已实现：

| 能力 | 实现状态 |
| --- | --- |
| 登录认证（账号密码、忘记密码、找回账号） | ✅ 已实现（前端 mock） |
| 应用授权（三级：企业/部门/用户） | ✅ 已实现 |
| 用户管理（CRUD、角色层级约束、密码管理） | ✅ 已实现 |
| 角色管理（权限树、冲突检测、审计） | ✅ 已实现 |
| 组织管理（企业、部门、权限下放） | ✅ 已实现 |
| 门户首页与应用中心 | ✅ 已实现 |
| 监控总览（结构就位，等待真实数据） | ✅ 结构已实现 |

尚未实现：

| 能力 | 说明 |
| --- | --- |
| Token 配额管理 | 配额配置、监控面板、配额日志页面均未实现 |
| 系统日志 / 操作日志页 | 日志表格查询页面未实现 |
| 资源看板 | 系统资源指标卡片页面未实现 |
| 个人中心 | 用户个人信息查看/修改页面未实现 |
| 我的 Token 用量 | 个人配额查看页面未实现 |
| 后端 API 接入 | 所有数据均为前端 mock + localStorage 持久化 |

### 3.5 当前 AI 应用数据

| 应用 | 类型 | 说明 |
| --- | --- | --- |
| 知识助手 | 办公协同 | 企业知识库检索与问答 |
| 会议纪要 | 办公协同 | 音频转写、会议摘要、待办提取 |
| 代码生成 | 研发提效 | 基于自然语言生成代码 |
| 代码审查 | 研发提效 | 代码风险识别与优化建议 |
| 电路审查 | 硬件设计 | 电路设计合规与风险检查 |
| 元器件库 | 硬件设计 | 元器件查询、选型与参数对比 |
| 文档生成 | 办公协同 | 生成技术文档、报告、方案等 |
| 文档审查 | 办公协同 | 文档校对、格式、逻辑和合规审查 |

### 3.6 演示账号与企业数据

演示企业：**平台**（超管所在）、**智研航空**、**云翔工业**、**航星智造**。

| 账号 | 角色 | 企业 / 部门 | 快捷密码 |
| --- | --- | --- | --- |
| `admin` 或 `admin1` | 超级管理员 | 平台 / 平台运营组 | admin |
| `admin2` | 企业管理员 | 智研航空 / 管理中心 | admin |
| `admin3` | 部门管理员 | 智研航空 / 研发中心 | admin |
| `user1` | 普通用户 | 智研航空 / 研发中心 | admin |
| `user2` | 普通用户 | 智研航空 / 硬件设计部 | admin |
| `user3` | 普通用户 | 智研航空 / 产品中心 | admin |

密码重置后为 `Avic@12345678`，登录页同时接受该密码。

## 4. 产品定位与目标

### 4.1 产品定位

AI 应用聚合平台是企业内部统一 AI 能力门户和运营治理平台，为员工提供一站式 AI 应用入口，为管理员提供权限、配额、监控、日志、审计等管理能力。

### 4.2 建设目标

- 统一入口：聚合企业内多个 AI 应用，提供搜索、分类、跳转和推荐能力。
- 统一身份：通过登录页和认证体系识别用户身份、企业、部门和角色。
- 统一权限：基于角色和权限树控制不同用户可访问的模块、功能和操作。
- 统一配额：按企业、部门、用户组、个人管理 Token 配额和使用情况。
- 统一监控：集中展示系统资源、服务状态、任务队列、API 调用和日志。
- 统一审计：记录权限变更、配额调整、登录行为、关键操作和异常事件。

## 5. 用户角色

| 角色 | 说明 | 关键权限 |
| --- | --- | --- |
| 普通用户 | 企业内部 AI 应用使用者 | 登录平台、访问授权应用 |
| 部门管理员 | 部门级管理人员 | 管理本部门普通用户 |
| 企业管理员 | 企业级管理员 | 管理本企业用户（部门管理员及以下）、角色权限配置 |
| 超级管理员 | 平台级管理员 | 跨企业管理、应用管理、全局配置、系统监控 |
| 运维人员 | 系统运行维护人员 | 查看监控总览 |

## 6. 信息架构

```text
AI 应用聚合平台
├─ 门户前台
│  ├─ 顶部导航（首页 / 应用中心 / 平台优势 / 搜索 / 用户下拉）
│  ├─ 门户首页 /
│  ├─ 应用中心 /apps
│  └─ 登录页 /login
├─ 用户下拉菜单（按角色过滤）
│  ├─ [头像 + 姓名 + 企业部门 + 角色标签]
│  ├─ 权限管理 → /admin/roles（enterprise_admin / super_admin）
│  ├─ 监控运维 → /ops/monitor（ops / super_admin）
│  └─ 退出登录
└─ 管理后台（AdminLayout：侧边栏 + 面包屑 + 内容区）
   ├─ 应用管理 /admin/apps（super_admin）
   ├─ 权限管理
   │  ├─ 角色管理 /admin/roles
   │  ├─ 用户管理 /admin/users
   │  └─ 组织管理 /admin/organizations（企业 / 部门 / 权限下放）
   └─ 监控运维
      └─ 监控总览 /ops/monitor
```

## 7. 功能需求

### 7.1 登录页（已实现）

#### 7.1.1 页面布局

- 左右分栏卡片（宽 980px，双列 Grid），桌面端展示；移动端单列（隐藏品牌面板）。
- 左侧品牌面板：深蓝渐变背景，Logo + 平台名称 + 企业级 AI 能力介绍 + 版权信息。
- 右侧登录表单：居中，标题"账号登录"，欢迎副标题。

#### 7.1.2 登录逻辑

- 账号密码登录，支持「记住我」。
- 连续失败 ≥2 次显示图形验证码（4 位，演示值 A8K3）；连续失败 ≥5 次提示账号锁定。
- 忘记密码和找回账号通过独立 Dialog 完成，不跳离登录页。

#### 7.1.3 忘记密码 Dialog（4 步 Stepper）

1. 输入邮箱 → 邮箱格式校验 → 发送验证码
2. 验证码 → 6 位数字校验 → 60 秒倒计时重发
3. 设置新密码 → 强度校验（≥8 位含大写 + 数字）+ 两次一致
4. 成功提示

#### 7.1.4 找回账号 Dialog

输入企业邮箱 → 区分单账号/多账号/未找到三种状态提示。

### 7.2 门户首页与应用矩阵（已实现）

#### 7.2.1 首页展示

- 顶部导航：Logo、导航链接、搜索框、用户区（未登录显示登录按钮，已登录显示 UserMenu）。
- Hero：全屏视窗高，深蓝渐变背景，平台标题（渐变文字）、副标题、两个 CTA 按钮。
- 应用矩阵：Tab 分类筛选 + 卡片 Grid（4列），登录后根据权限过滤；超级管理员看全部。
- 平台价值：4 列价值卡片。

#### 7.2.2 应用访问控制

- 未授权应用不在门户首页展示（不显示而非置灰）。
- 应用中心中未登录点击跳到登录页；已登录但未被授权的应用直接不在列表中出现（当前未显示申请权限入口）。

### 7.3 应用管理（已实现，仅 super_admin）

#### 7.3.1 应用 CRUD

新增/编辑应用：名称、分类、图标、描述、跳转网址（格式校验）、启停状态。

#### 7.3.2 应用授权

三级 Checkbox 树（企业 → 部门 → 用户），支持：
- 企业级全选/部分选/取消；部门级全选/部分选/取消；单人勾选。
- 「全部企业全选」和「全部清空」快捷操作。
- 授权人数 Chip（未授权 / N 人 / 全员可见）实时展示。
- 超级管理员始终可访问，不受授权列表影响。

### 7.4 用户管理（已实现）

#### 7.4.1 数据范围

| 当前角色 | 可见数据 | 可管理用户 |
| --- | --- | --- |
| 超级管理员 | 全部企业 | 全部用户（不含管自己） |
| 企业管理员 | 本企业 | 部门管理员、普通用户（不含运维人员） |
| 部门管理员 | 本部门 | 本部门普通用户 |

#### 7.4.2 操作能力

- 新增用户：角色选项受层级约束；企业/部门选项受数据范围约束（带锁定图标提示）。
- 编辑用户：账号不可修改，其余字段可编辑。
- 启停用户：Switch 直接切换（不能操作自己）。
- 重置密码：确认弹窗展示初始密码 `Avic@12345678`，提示用户登录后修改。
- 删除用户：二次确认，不能删除自己。
- 密码查看/复制（仅 super_admin 列可见）：眼睛图标查看/隐藏，复制图标复制到剪贴板。

### 7.5 角色管理（已实现）

#### 7.5.1 页面布局

左侧角色列表（带筛选栏）+ 右侧权限配置区（选中角色后展示权限树）。

#### 7.5.2 权限树（三级结构）

```text
权限
├─ 应用中心
│  ├─ 应用列表
│  │  ├─ 查看
│  │  ├─ 访问
│  │  └─ 申请权限
│  └─ 应用配置（查看 / 新增 / 编辑 / 删除）
├─ 权限管理
│  ├─ 用户管理（查看 / 新增 / 编辑 / 删除 / 重置密码）
│  └─ 角色管理（查看 / 新增 / 编辑 / 删除 / 分配权限）
├─ Token 配额管理
│  ├─ 配额配置（查看 / 调整 / 审批）
│  └─ 配额日志（查看 / 导出）
└─ 系统监控与运维
   ├─ 监控总览（查看）
   └─ 系统日志（查看 / 导出）
```

#### 7.5.3 权限冲突检测

系统在保存前检测冲突（如"删除用户 + 仅查看用户"互斥），阻止保存并高亮冲突项。

#### 7.5.4 保存流程

权限变更 → 弹出确认 Dialog（展示新增/移除权限条数和影响用户数）→ 管理员确认 → 写入 `aap.rolePermissions` → 权限实时生效（无需重新登录）。

#### 7.5.5 权限审计时间轴

弹出 Dialog，展示变更历史（时间、操作人、变更内容），支持筛选。

### 7.6 组织管理（已实现）

三个 Tab：企业管理 / 部门管理 / 权限下放。

#### 7.6.1 企业管理

列表字段：名称、编码、描述、联系人、状态、创建时间。支持新增、编辑、停用、删除；super_admin 可管理全部企业，enterprise_admin 仅查看本企业。

#### 7.6.2 部门管理

列表字段：名称、编码、所属企业、负责人、状态。支持新增、编辑、停用、删除；按企业筛选；enterprise_admin 只看本企业部门。

#### 7.6.3 权限下放（Delegate）

super_admin 可将特定权限集合下放给：

| 角色 | 说明 |
| --- | --- |
| 企业管理员 | 管理本企业用户、角色、配额 |
| 部门管理员 | 管理本部门用户与分配项 |
| 运维人员 | 查看监控、日志、告警 |

选择目标角色 → 勾选可下放权限 → 确认保存，调用 `setRolePermissions` 实时生效，页面刷新后权限仍保留（持久化到 `aap.rolePermissions`）。

### 7.7 监控总览（已实现结构）

- 4 张指标卡片：应用总数、注册用户数、总调用次数、活跃用户数（当前显示"—"，等待真实埋点数据接入）。
- 时间维度切换（今日 / 近 7 天 / 近 30 天）。
- 应用使用次数表：数据为空时显示带描述的空状态组件。
- 用户活跃度表：数据为空时显示带描述的空状态组件。

### 7.8 Token 配额管理（待建设）

#### 7.8.1 配额配置页 `/admin/quotas`

左侧树形导航（企业 → 部门 → 用户组 → 个人）+ 右侧配置面板。

配置字段：总 Token 配额、已用量、剩余量、预警阈值、生效周期（日/周/月/季度/年）、超额策略（阻断 / 审批后继续 / 降级模型 / 仅提醒）、调整原因。

#### 7.8.2 配额监控 `/admin/quotas/monitor`

仪表盘展示：各维度使用占比（环形/条形进度）、趋势折线图（日/周/月切换）、高消耗应用 Top 5、高消耗用户 Top 5。

预警阈值在图表中标注；超阈值黄色警示，配额用尽红色警示。

#### 7.8.3 配额日志 `/admin/quotas/logs`

时间轴展示：配额调整、预警触发、配额用尽、超额审批、系统重置等事件，支持筛选（事件类型 / 对象类型 / 操作人 / 时间范围 / 应用来源）。

### 7.9 系统日志（待建设）

#### 7.9.1 日志页 `/ops/logs`

表格布局，字段：时间、级别（INFO/WARN/ERROR）、服务、用户、企业、请求 ID、日志摘要、操作（查看详情 / 复制请求 ID）。

筛选：级别、关键词、时间范围、服务、企业、请求 ID。

展示规则：ERROR 整行红色背景，WARN 黄色背景，INFO 默认样式。

日志详情：完整堆栈、请求参数（脱敏）、上下游调用链。

#### 7.9.2 资源看板 `/ops/resources`

指标卡片（大字体 + 迷你趋势图）：CPU 使用率、内存使用率、磁盘使用率、网络流量、数据库连接数、缓存命中率、AI 服务延迟、Token 消耗速率。时间维度：实时 / 1 小时 / 24 小时。

## 8. 非功能需求

### 8.1 安全性

- 演示阶段密码以明文形式保存在前端数据中，仅用于演示；生产环境必须改为服务端哈希存储与鉴权。
- 所有后台接口接入后端后必须进行服务端鉴权，前端权限控制不能作为安全边界。
- 敏感字段（密码）在 UI 中脱敏展示，通过图标切换查看。
- 操作日志和审计日志不可由普通管理员删除。

### 8.2 性能

- 首页首屏加载时间目标小于 3 秒。
- 权限树在 1000 个权限节点内应保持流畅操作。
- 日志查询默认分页加载，避免一次性拉取大量数据。
- 监控图表需支持定时刷新，避免全量刷新造成页面卡顿。

### 8.3 可用性

- 关键操作需有明确确认（已实现：删除角色/用户/应用、停用用户、重置密码、保存权限变更）。
- 表单提供即时校验和明确错误提示。
- 管理页面支持空状态、加载态、失败态（空状态组件 `EmptyState` 已实现）。
- 权限不足时显示 Alert 提示，不出现空白页（已实现：应用管理非 super_admin 场景）。

### 8.4 可审计性

已实现的审计能力：
- 角色权限变更写入 `auditLog`（前端 mock），可在审计时间轴中查看。
- 权限下放操作实时写入 `aap.rolePermissions`。

待实现（后端接入后）：
- 登录成功/失败/退出登录。
- 用户/角色 CRUD 操作。
- Token 配额调整。
- 日志导出。

### 8.5 兼容性

- 支持主流 Chromium 内核浏览器。
- 支持 1366px 及以上桌面分辨率。
- 登录页和门户首页响应式适配移动端。
- 管理后台最低支持平板和桌面端（1024px+）。

### 8.6 界面一致性

- 主 UI 框架：Material UI 6 + Emotion，全局主题通过 Ant Design 6 ConfigProvider 注入（主色 `#1677ff`，圆角 6px）。
- 两套框架共存，MUI 组件使用 `sx` prop 覆盖，antd 组件通过 ConfigProvider token 控制；新开发页面优先选用 MUI。
- 门户首页使用深蓝渐变航空科技风格，管理后台使用白色侧边栏 + 浅灰内容区的工作台风格。
- 卡片圆角 8px，主色 `#1677ff`，危险色 `#ff4d4f`，成功色 `#52c41a`，警告色 `#faad14`。

## 9. 权限模型

### 9.1 RBAC 模型

```text
用户 → 角色 → 权限（基础矩阵 + 动态下放叠加）
```

基础权限矩阵（`PERMISSION_MATRIX`）在 `src/auth/AuthContext.tsx` 中定义；动态下放权限通过 `setRolePermissions` 写入 `aap.rolePermissions`，两者取并集。

### 9.2 权限编码规范

```text
模块:功能:操作
```

示例：
```text
app:center:view          应用中心查看
app:center:access        应用访问
user:manage:create       用户新增
user:manage:delete       用户删除
role:manage:assign_permission  角色权限分配
quota:config:update      配额调整
monitor:log:export       日志导出
monitor:overview:view    监控总览查看
```

### 9.3 数据权限

| 角色 | 数据范围 |
| --- | --- |
| 超级管理员 | 全平台所有企业数据 |
| 企业管理员 | 本企业数据（跨企业操作被锁定） |
| 部门管理员 | 本部门数据 |
| 普通用户 / 运维人员 | 仅个人数据（运维可查看监控数据） |

## 10. 数据持久化

业务数据通过 `src/data/store.ts` 的 `usePersistedState` 写入 `localStorage`：

| Key | 内容 |
| --- | --- |
| `aap.session` | 当前登录用户信息 |
| `aap.rolePermissions` | 角色动态权限覆盖层 |
| `aap.store.apps` | 应用列表（含授权用户 ID 数组） |
| `aap.store.users` | 用户列表 |
| `aap.store.roles` | 角色列表 |
| `aap.store.enterprises` | 企业列表 |
| `aap.store.departments` | 部门列表 |

重置演示数据（浏览器控制台）：
```js
Object.keys(localStorage).filter(k => k.startsWith('aap.')).forEach(k => localStorage.removeItem(k));
location.reload();
```

## 11. 关键流程

### 11.1 登录流程

```text
用户进入 /login → 输入账号密码 → resolveAccount 匹配 MOCK_USERS
→ 校验密码（接受 'admin' 快捷密码 或 实际密码）
→ 校验账号状态（disabled 提示禁用）
→ 写入 aap.session → 路由跳转（from 参数 → 门户首页）
→ 根据角色展示可访问应用和 UserMenu 菜单项
```

### 11.2 权限分配流程

```text
管理员进入 /admin/roles → 选中角色 → 勾选权限树
→ 系统检测冲突（阻止保存 + 高亮冲突项）
→ 通过后弹出确认 Dialog（展示变更摘要和影响用户数）
→ 确认 → 写入 aap.rolePermissions → 权限实时生效
→ 写入 auditLog（审计时间轴可查）
```

### 11.3 应用授权流程

```text
super_admin 进入 /admin/apps → 点击应用行「授权」按钮
→ 打开三级树 Checkbox Dialog（企业/部门/用户）
→ 勾选被授权用户 → 保存
→ 前端 apps store 更新 permittedUserIds
→ 对应用户刷新后可在首页/应用中心看到该应用
```

## 12. 页面清单

| 页面 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| 门户首页 | `/` | ✅ 已实现 | 品牌首页、应用矩阵、平台价值；登录后按权限过滤应用 |
| 登录页 | `/login` | ✅ 已实现 | 账号密码登录、忘记密码/找回账号弹窗、验证码、错误次数限制 |
| 应用中心 | `/apps` | ✅ 已实现 | 授权应用列表，支持搜索、分类筛选、排序；权限访问控制 |
| 应用管理 | `/admin/apps` | ✅ 已实现 | 仅 super_admin；应用 CRUD、启停、三级授权 |
| 用户管理 | `/admin/users` | ✅ 已实现 | 用户 CRUD、角色层级约束、密码管理、启停、分页 |
| 角色管理 | `/admin/roles` | ✅ 已实现 | 角色列表 + 权限树配置；冲突检测；审计时间轴 |
| 组织管理 | `/admin/organizations` | ✅ 已实现 | 企业管理、部门管理、权限下放（三 Tab） |
| 监控总览 | `/ops/monitor` | ✅ 结构已实现 | 指标卡 + 应用使用表 + 活跃度表；等待真实数据接入 |
| Token 配额配置 | `/admin/quotas` | ⬜ 待建设 | 层级树 + 配置面板 |
| Token 配额监控 | `/admin/quotas/monitor` | ⬜ 待建设 | 配额使用仪表盘 |
| Token 配额日志 | `/admin/quotas/logs` | ⬜ 待建设 | 配额事件时间轴 |
| 资源看板 | `/ops/resources` | ⬜ 待建设 | 系统资源指标卡片 |
| 系统日志 | `/ops/logs` | ⬜ 待建设 | 日志表格、筛选、详情 |
| 个人中心 | `/profile` | ⬜ 待建设 | 个人信息查看/修改 |
| 我的 Token 用量 | `/profile/quota` | ⬜ 待建设 | 个人配额查看 |

## 13. 迭代建议

### 已完成（第一、二阶段）

- ✅ 登录认证、忘记密码、找回账号
- ✅ 门户首页与应用矩阵（按权限过滤）
- ✅ 应用管理（CRUD + 三级授权）
- ✅ 用户/角色/组织管理
- ✅ 权限树配置、冲突检测、审计时间轴
- ✅ 权限下放
- ✅ 监控总览结构

### 下一阶段（第三阶段：Token 配额）

- 建设 `/admin/quotas` 配额配置（树形导航 + 配置面板）
- 建设 `/admin/quotas/monitor` 配额监控仪表盘
- 建设 `/admin/quotas/logs` 配额日志时间轴
- 接入 Token 消耗统计

### 第四阶段：监控运维完善

- 建设 `/ops/resources` 资源看板
- 建设 `/ops/logs` 系统日志（表格 + 筛选 + 详情抽屉）
- 接入真实监控数据源，替换 mock

### 第五阶段：个人功能 + 后端接入

- 建设个人中心 `/profile`
- 建设我的 Token 用量 `/profile/quota`
- 完善 UserMenu 入口（增加个人中心、我的 Token 用量、操作日志）
- 全面替换 mock 数据为后端 API
- 完善审计日志（登录/CRUD 操作全量写入）

## 14. 风险与约束

- 当前所有业务数据均为 mock + `localStorage` 持久化，清除浏览器数据后重置。
- 演示密码以明文形式保存在前端代码中，仅用于演示，生产环境必须改为服务端哈希存储与鉴权。
- 角色权限和 Token 配额均需要后端强校验，前端展示不能作为安全边界。
- 监控与日志依赖实际运维体系，需要明确指标采集来源和日志存储方案。
- 双 UI 框架（MUI 6 + antd 6）并存，新开发页面建议统一使用 MUI，避免引入更多 antd 组件以减少包体积。
- 若需要接入企业 SSO，需提前确认协议类型（OAuth2、OIDC、SAML 或企业微信、钉钉等）。
