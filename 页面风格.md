# 页面风格说明

来源文件：`本体建模核心与选题方案-机载0604-独立版.html`

## 1. 风格定位

该页面是一个全屏演示型页面，整体定位为“企业级科技汇报 / 航空工业数字化方案演示”。视觉语言偏理性、克制、专业，强调结构化信息表达和方案可信度，而不是营销型网页的强情绪化视觉。

核心关键词：

- 企业级科技感
- 航空蓝白基调
- 玻璃拟态卡片
- 数据看板式信息组织
- 结构化汇报 / 咨询方案风
- 轻量动效与演示导航

## 2. 整体页面形态

页面采用 PPT 式全屏幻灯片结构：

- 容器占满 `100vw / 100vh`。
- 每一页是一个 `.presentation-slide`，通过绝对定位叠放。
- 当前页通过 `.active` 控制显隐。
- 页面切换使用淡入、上移复位、轻微缩放的过渡动画。
- 顶部有细进度条，底部左侧有页码，底部右侧有键盘操作提示。

这种形态适合方案汇报、产品路演、业务复盘、技术概念宣讲，不适合长滚动内容页。

## 3. 色彩体系

主色体系来自 “Lingjing Core” 设计令牌，整体是浅色科技风。

主色：

- 航空蓝：`#0066CC`，用于品牌主色、关键标题、实体/流程节点。
- 脉冲青：`#0099AA`，用于强调、进度、论点角标、连接关系。
- 信号绿：`#00A383`，用于推荐、成功、可行方案、正向结论。
- 警示琥珀：`#D97706`，用于风险、提醒、备选或问题态。
- 错误红：`#DC2626`，用于传统模式问题、失败、阻塞。
- 信息靛蓝：`#4338CA`，作为辅助信息色。

中性色：

- 页面浅背景：`#EFF6FF`
- 浅灰背景：`#F8FAFC`、`#F1F5F9`、`#E2E8F0`
- 正文灰：`#475569`
- 次级文字：`#64748B`
- 主标题深色：`#0F172A` / `#1E293B`

使用原则：

- 大面积背景保持浅蓝灰和白色，不使用高饱和大色块。
- 蓝、青、绿作为主视觉信号，橙、红只用于风险和问题。
- 同一页通常只使用 1 个主强调色，其他颜色作为状态区分。
- 卡片内部可以用极浅色底表达语义，例如 `rgba(0,153,170,0.06)`、`#F0FDF6`、`#FFFBEB`。

## 4. 背景与空间氛围

背景不是纯白，而是浅灰蓝渐变：

- 使用 `linear-gradient(135deg, neutral-50, neutral-200, neutral-300)`。
- 背景尺寸为 `200% 200%`，有 20 秒循环的缓慢流动动画。
- 叠加 3 个大面积模糊圆形光斑：
  - 蓝色光斑在右上。
  - 青色光斑在左下。
  - 绿色光斑在中心附近。
- 光斑使用 `filter: blur(80px)` 和低透明度，作为氛围层，不抢内容。

视觉效果接近“干净的科技会议室投屏”，不是暗黑赛博风。虽然 CSS 中保留了暗色主题和 UE 科技卡片变量，但当前文档实际使用的是浅色主题。

## 5. 布局规则

基础布局：

- 页面内容最大宽度约 `1400px`。
- 幻灯片外层留白较大，桌面端有明显呼吸感。
- 常见布局是顶部标题区 + 中部内容区。
- 内容区以 Flex 和 Grid 为主，常见 2 列、3 列、4 列结构。
- 页面元素尽量居中对齐，重点页使用中心构图。

典型结构：

```html
<div class="presentation-slide">
  <div class="slide-content">
    <div class="slide-header">
      <h2 class="slide-title">页面标题</h2>
      <p class="slide-subtitle">页面副标题</p>
    </div>
    <!-- 卡片 / 对比 / 流程 / 网格内容 -->
  </div>
</div>
```

信息密度：

- 单页信息量偏高，但通过卡片、标签、色带、图标拆分。
- 重要结论通常居中或放入独立胶囊/强调框。
- 对比类页面采用左右分栏，中间用箭头、VS、分隔线强化逻辑。

## 6. 排版风格

字体：

- 标题字体：`Kingsoft Cloud`、`Noto Sans SC`。
- 二级标题与正文：`Source Han Mono CN`、`Noto Sans SC`。
- 页码等辅助信息使用等宽字体：`JetBrains Mono` / `Source Han Mono CN`。

字号层级：

- 封面主标题：`clamp(2rem, 5vw, 3.25rem)`，粗重。
- 普通页标题：约 32px-40px，粗体。
- 副标题：约 20px-24px，较浅灰色。
- 正文：12px-16px，根据卡片密度调整。
- 标签与角标：11px-12px，粗体、大写、增加字距。

排版特征：

- 标题权重大，正文克制。
- 小标签常用 uppercase、letter-spacing，形成“系统标签 / 看板标识”气质。
- 大段正文较少，更多使用短句、关键词、节点式表达。
- 中英文混排常见，例如 `LLM`、`System 1`、`ENTITY`、`AND / OR`。

## 7. 卡片组件

核心组件是玻璃拟态卡片：

- 背景：`rgba(255,255,255,0.98)` 或近似白色半透明。
- 模糊：`backdrop-filter: blur(16px)`。
- 边框：浅蓝透明边框 `rgba(28,102,196,0.15)`。
- 阴影：轻量蓝色阴影 `0 8px 32px rgba(28,102,196,0.08)`。
- 圆角：以 12px-16px 为主。
- 内边距：常用 24px-32px。
- Hover：轻微上浮 `translateY(-4px)`，阴影增强。

卡片变体：

- 左侧色条卡片：用于分类、重点论点、优先级。
- 顶部色条卡片：用于多列并列方案、能力模块。
- 对比卡片：左侧灰/红表达传统问题，右侧青/绿表达新方案。
- 信息块卡片：白底、小图标、标题、说明文字组合。
- 胶囊强调框：用于结论句、推荐标签、页内总结。

复用建议：

- 普通说明卡片用白色玻璃底。
- 强结论卡片使用青色或绿色边框/色条。
- 风险提示使用浅橙背景和琥珀边框。
- 传统/不推荐内容使用灰底、低饱和图标和红色问题点。

## 8. 标签与徽章

页面大量使用小型标签来表达状态和层级。

常见类型：

- 论点角标：左上角绝对定位，青色半透明背景。
- 阶段角标：右上角绝对定位，圆角胶囊。
- 优先级徽章：`推荐首选`、`次选`、`备选`、`后期扩展` 等。
- 关键词标签：小胶囊、浅色底、细边框、粗体。
- 分区标签：用于“第二部分”等章节页。

视觉规则：

- 标签高度较小，文字加粗。
- 胶囊圆角使用 `9999px`。
- 背景色透明度通常 0.10-0.18。
- 边框透明度通常 0.20-0.30。
- 不使用实心大色块，保持轻量。

## 9. 图标与信息表达

图标库使用 Lucide：

```html
<script src="https://unpkg.com/lucide@latest"></script>
<i data-lucide="briefcase"></i>
```

图标风格：

- 线性图标，尺寸多为 14px、16px、20px、24px、28px。
- 常放入浅色圆形或圆角方形底座。
- 图标颜色与模块语义色一致。
- 图标主要用于辅助识别，不承担装饰主视觉。

常见图标语义：

- `briefcase` / `calendar`：封面元信息。
- `database` / `hard-drive`：传统系统、数据。
- `cpu` / `brain` / `sparkles`：AI、本体、智能能力。
- `git-branch` / `route`：关系、因果、路径。
- `alert-triangle` / `alert-circle`：问题和风险。
- `check` / `check-circle`：适合、推荐、正向结果。

## 10. 页面内容模式

该文档不是普通网页内容，而是“论点推进式演示”。

常见页面模式：

- 封面页：品牌小字 + 大标题 + 副标题 + 日期/客户元信息。
- 回顾页：4 个并列卡片，总结上一轮关键判断。
- 论点页：左上角论点角标 + 主标题 + 结构化论证。
- 对比页：传统方式 vs 本体方式，左灰/红，右青/绿。
- 概念拆解页：实体、关系、行动等 3 列卡片。
- 流程页：节点 + 箭头 + 结果框。
- 方案页：问题、方案、实施路线、推荐理由。
- 收束页：大标题 + 中心结论卡片。
- 团队页：数字指标 + 简短能力说明。

内容写法：

- 标题直接给观点，不只写名词。
- 副标题补充判断或业务语境。
- 卡片标题用短句，说明文字控制在 1-2 行。
- 使用箭头、加号、VS、编号强化逻辑推进。
- 结论句用青色或绿色强调。

## 11. 动效与交互

主要动效：

- 页面背景缓慢流动。
- 模糊光斑缓慢漂浮。
- 幻灯片进入时淡入、上移、缩放复位。
- 卡片 hover 轻微上浮。
- 顶部进度条随页码变化。

交互方式：

- 方向键左右/上下切换页面。
- `F` 键进入全屏。
- 底部显示页码和操作提示。

动效原则：

- 动效服务演示节奏，不制造强干扰。
- 背景动效低透明、慢速。
- 卡片 hover 幅度小，偏专业工具感。
- 过渡时间可以控制在 300ms-800ms。

## 12. 响应式策略

CSS 中包含响应式规则，但页面主要面向桌面投屏。

响应式特征：

- 1024px 以下，四列网格变两列。
- 640px 以下，多列网格变单列。
- 复杂左右分栏在移动端应改为上下堆叠。
- 封面标题在小屏下减小字号。

如果复用到 Web 应用中，需要额外注意：

- 当前很多页面内容依赖固定视口和横向空间。
- 移动端不要强行保留 3-4 列结构。
- 大量内联样式需要组件化，否则后续维护成本高。

## 13. 可复用设计令牌

建议在新页面中保留以下核心令牌：

```css
:root {
  /* 主色 */
  --color-aero-blue:    #0066CC;
  --color-blue-hover:   #3B88E8;
  --color-pulse-cyan:   #0099AA;
  --color-signal-green: #00A383;
  --color-warning:      #D97706;
  --color-error:        #DC2626;

  /* 紫色强调（克制使用） */
  --color-purple:       #7C3AED;
  --color-purple-bg:    rgba(124, 58, 237, 0.08);
  --color-purple-glow:  rgba(124, 58, 237, 0.20);

  /* 背景 */
  --theme-bg-base:      #F2F5FF;
  --theme-bg-soft:      #EEF2FF;

  /* 文字 */
  --text-primary:       #0F1729;
  --text-secondary:     #4A5568;
  --text-muted:         #94A3B8;

  /* 玻璃卡片 */
  --bg-glass:           rgba(255, 255, 255, 0.85);
  --glass-border:       1px solid rgba(99, 102, 241, 0.12);
  --glass-shadow:       0 8px 32px rgba(99, 102, 241, 0.07), 0 2px 8px rgba(0, 0, 0, 0.04);
  --glass-shadow-raise: 0 16px 48px rgba(99, 102, 241, 0.12), 0 4px 16px rgba(0, 0, 0, 0.06);
  --glass-blur:         blur(20px) saturate(160%);

  /* 圆角 */
  --radius-md:   8px;
  --radius-lg:   12px;
  --radius-xl:   16px;
  --radius-full: 9999px;

  /* 间距 */
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
}
```

## 14. 复刻页面时的设计建议

应该保持：

- 蓝白浅色科技基调。
- 玻璃拟态卡片和轻阴影。
- 小标签、色条、图标底座这些高频视觉元素。
- 标题先行、卡片拆解、结论收束的表达方式。
- 克制的动效和清晰的层级。

应避免：

- 大面积深色赛博风，除非明确切换到暗色主题。
- 大量渐变文字或高饱和背景。
- 圆角过大导致卡通化。
- 卡片套卡片过多，造成层级混乱。
- 长段正文直接铺满页面。
- 将所有强调都做成蓝色，导致信息状态不可区分。

## 15. 一句话总结

这个 HTML 的页面风格可以概括为：以浅色航空科技感为基底，用玻璃拟态卡片承载高密度业务信息，通过蓝、青、绿、橙、红的语义色和 Lucide 线性图标组织论证节奏，形成适合企业级方案汇报的全屏演示界面。

---

## 16. React 项目实际应用说明（旧版玻璃拟态方案，已废弃）

> ⚠️ 本节为历史记录。当前生效方案见 **第 17 节：Bento 便当格设计系统**。

### 16.1 字体

- 标题/品牌名：`Syne`（Google Fonts，通过 `index.html` 引入）
- 正文/界面：`DM Sans`（Google Fonts，通过 `index.html` 引入）
- 回退链：`-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif`

### 16.2 色彩系统（已应用到项目）

| 角色 | 值 |
|------|-----|
| 主蓝 | `#0066CC` |
| 主蓝悬停 | `#3B88E8` |
| 紫色强调（克制） | `#7C3AED` |
| 青色 | `#0099AA` |
| 成功绿 | `#00A383` |
| 警告橙 | `#D97706` |
| 错误红 | `#DC2626` |
| 页面底色 | `#F2F5FF` |
| 次级底色 | `#EEF2FF` |
| 主文字 | `#0F1729` |
| 次文字 | `#4A5568` |

### 16.3 紫色使用规范

紫色 `#7C3AED` 仅在以下位置出现，其他地方不使用：

- 品牌 Logo Mark：蓝→紫渐变 `linear-gradient(135deg, #0066CC, #7C3AED)`
- Hero 标题渐变末段：`#C4B5FD`（极淡紫）
- Login 左侧面板标题渐变末段：`#C4B5FD`
- Login 左侧面板紫色光晕 orb（`::before` 伪元素，opacity ≈ 0.22）
- Hero 区域右上角 `::after` 紫色背景光斑（opacity ≈ 0.14）
- 卡片阴影中混入 `rgba(99,102,241,0.07-0.12)` 极淡紫色

### 16.4 玻璃拟态规范

```css
background: rgba(255, 255, 255, 0.85);
backdrop-filter: blur(20px) saturate(160%);
border: 1px solid rgba(99, 102, 241, 0.12);
box-shadow: 0 8px 32px rgba(99, 102, 241, 0.07), 0 2px 8px rgba(0, 0, 0, 0.04);
border-radius: 16px;
```

Hover 时：
```css
box-shadow: 0 16px 48px rgba(99, 102, 241, 0.12), 0 4px 16px rgba(0, 0, 0, 0.06);
transform: translateY(-4px);
background: rgba(255, 255, 255, 0.95);
```

### 16.5 Nav 导航栏

```css
background: rgba(242, 245, 255, 0.88);
backdrop-filter: blur(20px) saturate(160%);
border-bottom: 1px solid rgba(99, 102, 241, 0.12);
box-shadow: 0 4px 20px rgba(99, 102, 241, 0.06);
```

### 16.6 文件变更记录

| 文件 | 改动内容 |
|------|---------|
| `index.html` | 引入 DM Sans + Syne Google Fonts |
| `src/main.tsx` | ConfigProvider 主题 token 换为新色彩系统 |
| `src/styles/global.css` | 全量设计令牌更新；Nav、Hero、卡片、Login、Admin 样式升级 |
| `src/pages/Home/HeroBanner.tsx` | 标题渐变加入淡紫尾段（`#C4B5FD`） |
| `src/pages/Home/AppMatrix.tsx` | 移除 section-title 内联渐变，由 CSS 统一控制 |
| `src/pages/Home/ValueSection.tsx` | 移除 section-title 内联渐变，由 CSS 统一控制 |
| `src/components/AdminLayout.tsx` | 侧边栏/顶栏 Logo 改为蓝→紫渐变；背景改为玻璃拟态；选中色更新 |
| `src/pages/Login/index.tsx` | 背景、左侧面板渐变更新；标题渐变加入淡紫尾段 |

---

## 17. Bento 便当格设计系统（当前生效方案）

> 2026-06 整体布局推倒重做。设计来源：`layout-preview-2-bento.html`（另有 `layout-preview-1-workspace.html`、`layout-preview-3-command.html` 两个备选布局预览，以及更早的三个风格预览 `design-preview-A/B/C-*.html`，均保留作参考）。

### 17.1 设计定位

- **布局范式**：放弃"居中 Hero + 四列等宽卡片"的传统营销页骨架，改为非对称首屏 + 大小混排便当格（Bento Grid）。
- **气质**：暖白底色 + 高饱和点缀色 + 大圆角 + 扁平填色，介于"企业级稳重"与"现代 SaaS 活泼"之间。
- **关键词**：Bento Grid、悬浮胶囊导航、非对称排版、品类语义色、墨黑点缀。

### 17.2 字体

| 用途 | 字体 |
|------|------|
| 标题/正文（拉丁字符） | `Bricolage Grotesque`（Google Fonts） |
| 标签/代号/数据 | `IBM Plex Mono` |
| 中文回退 | `PingFang SC` → `Microsoft YaHei` → `Source Han Sans CN` |

### 17.3 色彩令牌（global.css `:root`）

| 令牌 | 值 | 用途 |
|------|------|------|
| `--c-bg` | `#F6F5F2` | 页面底色（暖米白） |
| `--c-card` | `#FFFFFF` | 卡片 |
| `--c-ink` | `#16161A` | 墨黑：激活态、品牌块、统计带 |
| `--c-line` | `#E7E5E0` | 边框 |
| `--c-muted` / `--c-faint` | `#5F6470` / `#9B9FA8` | 次级/弱文字 |
| `--c-blue` | `#2D5BFF` | 主色（电光蓝），软底 `#E9EEFF` |
| `--c-violet` | `#7C3AED` | 研发提效品类，软底 `#F1EAFE` |
| `--c-teal` | `#0E9F8A` | 硬件设计品类，软底 `#E2F6F2` |
| `--c-amber` | `#E08700` | 文档/告警，软底 `#FCF1DD` |
| `--r-lg / --r-md / --r-sm` | `20px / 14px / 10px` | 圆角体系 |

### 17.4 核心布局结构

1. **悬浮胶囊导航**（`.top-nav`）：固定顶部 16px 处，圆角 18px 白色毛玻璃胶囊，菜单项为圆角胶囊（激活=墨黑底白字）。
2. **非对称首屏**（`.hero-grid`，1.15fr / 0.85fr）：
   - 左：badge 胶囊 + 大标题（关键词带蓝色荧光笔下划线 `.u`）+ 副标题 + 双 CTA；
   - 右：2×2 数据迷你便当格（`.hero-bento`：墨黑可用性卡 / 蓝色应用数卡 / 琥珀企业卡 / 白色运行状态卡）。
3. **应用便当格**（`.bento`，4 列 × 152px 行高，`grid-auto-flow: dense`）：
   - 「全部」视图：第 1 个应用占 2×2 大格（蓝底白字 + 对话演示 `.chatmock`），第 2 个占 2×1 横格（声波动画 `.wave`），其余 1×1，末尾追加墨黑「全部 N 个应用」跳转格；
   - 分类筛选视图：统一 1×1。
4. **统计带**（`.stats-band`）：墨黑圆角横带，4 个大数字。
5. **平台优势**（`.values-grid`）：左侧吸顶标题（`position: sticky`）+ 右侧编号列表项（hover 右移 6px）。
6. **页脚**：居中等宽小字。

### 17.5 管理后台（AdminLayout）

- 侧边栏：暖白 `#FBFAF8`，宽 236px；
- Logo：墨黑圆角方块 + 神经网络 SVG 记号 + `ADMIN CONSOLE` 等宽小标；
- 分组标题：IBM Plex Mono 全大写 9.5px，letter-spacing 2px，可折叠；
- 菜单项：28px 品类色图标磁贴 + 13px 文字，圆角 11px；**激活态 = 墨黑胶囊（白字 + 投影），磁贴变半透明白**；
- 顶栏：暖白毛玻璃 sticky 顶栏（面包屑 + 用户信息）。

### 17.6 登录页

- 页面底色 `#F6F5F2`，卡片 20px 圆角；
- 左侧品牌面板：纯电光蓝 `#2D5BFF`，右下角两个旋转大圆角方块装饰（CSS ::before/::after）；
- 右侧表单："欢迎回来 👋" 左对齐标题，登录按钮 13px 圆角带蓝色投影。

### 17.7 关键实现注意

- **禁用渐变文字**（`-webkit-text-fill-color: transparent`）——历史上曾导致标题在背景上不可见；标题一律实色。
- `antd-shim/material.tsx` 依赖 `--antx-*` 变量，global.css 末尾保留别名映射到新 `--c-*` 令牌。
- 便当格混排只在「全部」筛选且应用数 ≥ 4 时启用，分类视图回退为等宽格，保证动态数据安全。
- 入场动画用 `.fade-up .fd-1/2/3`，并带 `prefers-reduced-motion` 降级。

### 17.8 文件变更记录

| 文件 | 改动 |
|------|------|
| `index.html` | 字体换为 Bricolage Grotesque + IBM Plex Mono |
| `src/main.tsx` | ConfigProvider：主色 `#2D5BFF`、底色 `#F6F5F2`、圆角 10 |
| `src/styles/global.css` | 全量重写为 Bento 设计系统 v3 |
| `src/pages/Home/index.tsx` | 新增 StatsBand 统计带 + 页脚 |
| `src/pages/Home/TopNav.tsx` | 悬浮胶囊导航；登录按钮墨黑胶囊 |
| `src/pages/Home/HeroBanner.tsx` | 重写为非对称首屏 + 2×2 数据便当格 |
| `src/pages/Home/AppMatrix.tsx` | 重写为便当格混排（大格/横格/标准格/跳转格） |
| `src/pages/Home/ValueSection.tsx` | 重写为吸顶标题 + 编号列表 |
| `src/components/AdminLayout.tsx` | 暖白侧边栏 + 品类色磁贴 + 墨黑激活胶囊 |
| `src/pages/Login/index.tsx` | 蓝色品牌面板 + 圆角表单，移除渐变文字 |
| `src/pages/Apps/index.tsx` | 标题改为 eyebrow + 实色标题 |
